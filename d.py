def createTeam(firstIndex, secondIndex, isRed,
               first = 'DefenseAgent', second = 'AttackAgent'):

    if self.red:
     x = gameState.getWalls().width/4
    else:
     x = 3*gameState.getWalls().width/4
